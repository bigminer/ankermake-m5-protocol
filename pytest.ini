[pytest]
markers =
    browser: browser/UI tests
    live_printer: requires the configured running printer stack
    heating: changes nozzle/bed temperatures
    motion: moves axes or homes the printer
    print_job: uploads/starts/stops a print
    g36: enables and validates the pre-print G36 path
